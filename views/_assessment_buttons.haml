%div{:class=>"btn-group btn-group-sm", :role=>'group'}
  - if auth_to("assessment_admin") or assessment_belongs_to(assessment.id, session['user_id'])
    %a{:role=>:button, :href=>url("/assessment/#{assessment.id}/edit"), :class=>"btn btn-default"}
      %span{:class=>"glyphicon glyphicon-pencil"}
      =t(:Edit)
  - if auth_to("assessment_admin") or assessment_belongs_to(assessment.id, session['user_id'])
    %a{:role=>:button, :href=>url("/assessment/#{course.id}/delete"), :class=>"btn btn-danger"}
      %span.glyphicon.glyphicon-trash
      =t(:Delete)